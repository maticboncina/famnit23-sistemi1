; Solved with ChatGPT, because I lost the files. May not be correct.

; Let's assume AH = x and AL = y initially
MOV A, AX 
; Move the combined x and y values into register A

MOV B, AL 
; Move y into B
; Now A = x and B = y